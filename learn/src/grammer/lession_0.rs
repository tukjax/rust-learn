use ferris_says::say; // from the previous step
use std::io::{stdout, BufWriter};

fn main() {

    let mut x = "  ";

    let mut  x = 3;

    println!("The value of x is: {}",x);



    //e.g. tuple
    let mut tupleTest:(u32,u32,u32)  = (1,2,3);

    println!("{}",tupleTest.0)

}